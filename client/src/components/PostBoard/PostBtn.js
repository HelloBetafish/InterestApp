import React from "react";

export const PostBtn = props =>
<button type="button" id="keep" onClick={props.addPost} className="btn btn-warning" style={{marginLeft:"425px", marginTop:"5px"}}>
  post
</button>;